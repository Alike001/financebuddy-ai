export default function Settings() {
  return (
    <section className="page">
      <div className="page-head">
        <h2 className="page-title">Settings</h2>
        <p className="page-sub">Profile, theme, data, and live AI mode — coming online in step 4.</p>
      </div>
      <div className="placeholder-card">Settings form will appear here.</div>
    </section>
  );
}
