# USER

> This file is regenerated from the live store on every agent run.
> The string in `index.js` is just the static template — see `renderUserMd()`.

Name: {{name}}
Currency: {{currency}}
Monthly income: {{monthlyIncome}}
Monthly savings goal: {{savingsGoal}}
Today: {{today}}

Enabled skills: {{enabledSkills}}
Live AI mode: {{liveAi}}
