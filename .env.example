# FinanceBuddy AI runs entirely in the browser by default — no env vars needed.
#
# Optional: if you turn on "Live AI Mode" in Settings, you can paste an Anthropic
# API key directly into the app. The key is stored in your browser's localStorage
# only (never sent anywhere except api.anthropic.com).
